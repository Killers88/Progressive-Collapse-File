package edu.mit.civil.blastassessment.calculation;

public class ReflectedPressurePr {

	// TODO Complete Graphical Representation
	public static double findingPrWithAngleOfIncidence(final double z,
			final double Angle) {

		return 20;

	}
}

// if (z < 0) {
// return (20 * Angle);
// } else if ((z > 0.45) && (z < 0.7)) {
// return (20 * Angle);
// } else if ((z > 0.45) && (z < 0.7)) {
// return (20 * Angle);
// } else if ((z > 0.45) && (z < 0.7)) {
// return (20 * Angle);
// } else if ((z > 0.45) && (z < 0.7)) {
// return (20 * Angle);
// } else if ((z > 0.45) && (z < 0.7)) {
// return (20 * Angle);
// }
// return Angle;